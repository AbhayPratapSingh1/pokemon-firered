import * as THREE from "three";

// --- Building footprint ----------------------------------------------------
export const FLOOR_WIDTH = 16;
export const FLOOR_DEPTH = 14;
export const WALL_HEIGHT = 10.0;
export const WALL_THICK = 0.2;
export const DOOR_HALF_WIDTH = 1.1;
export const DOOR_HEIGHT = 2.4;
export const HALF_W = FLOOR_WIDTH / 2;
export const HALF_D = FLOOR_DEPTH / 2;

// --- World position --------------------------------------------------------
export const HOUSE_ORIGIN = new THREE.Vector3(500, 0, 300);
